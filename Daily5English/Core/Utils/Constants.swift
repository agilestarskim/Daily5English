import Foundation

enum Constants {
    enum Storage {
        static let wordsKey = "saved_words"
    }
    
    enum UI {
        static let cornerRadius: CGFloat = 10
        static let padding: CGFloat = 16
    }
} 